# InsertAnything Sim-to-Real Checkpoints

Place deployment checkpoints for `run_multi_episode_closed_loop.py` in this directory when running local real-robot experiments.

Large checkpoint files such as `.pth`, `.pt`, and `.ckpt` are intentionally ignored by git.
